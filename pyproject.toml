[project]
name = "snap-auto"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "AshweejaKR", email = "ashweejabhat23@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "playwright>=1.62.0",
    "python-dotenv>=1.2.2",
]

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-root = ""
